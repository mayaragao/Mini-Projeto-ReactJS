import React from 'react';
import './styles.css';
/* Criando componentes no react com funções, em vez de utilizar render e definir a classe Header */
const Header = () => (
<header id="main-header">MiniProjeto</header>
);

export default Header;